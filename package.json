{
  "name": "rectangular-labs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "type": "module",
  "scripts": {
    "clean": "vp run -r clean && git clean -xdf node_modules .sst",
    "dev:www": "docker compose up -d && vp run --cache -r build && vp run www#dev",
    "env:set": "pnpm dotenvx set",
    "env:get": "pnpm dotenvx get",
    "new:package": "vp create @rectangular-labs/create-package && find apps packages tooling -type d -name .vscode -prune -exec rm -rf '{}' +",
    "changeset": "changeset",
    "changeset:release": "vp run build && changeset publish",
    "postinstall": "pnpx sherif@latest",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@dotenvx/dotenvx": "^1.57.1",
    "@rectangular-labs/typescript": "workspace:*",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.33.0"
}
