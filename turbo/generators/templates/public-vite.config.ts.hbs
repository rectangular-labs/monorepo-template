import { defineConfig } from "vite-plus";

export default defineConfig(({ command }) => ({
  pack: {
    dts: {
      tsgo: true,
    },
    exports: {
      enabled: true,
      devExports: true,
    },
    entry: ["./src/index.ts"],
    format: ["esm" as const],
    sourcemap: "hidden",
    minify: command === "build",
    clean: true,
    failOnWarn: "ci-only",
    publint: true,
  },
}));
